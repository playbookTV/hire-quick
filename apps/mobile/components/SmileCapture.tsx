import { Text } from '../theme/restyle.js';
import type { SmileCaptureProps } from '../lib/kyc.js';
export default function SmileCapture(_props: SmileCaptureProps) {
  return <Text variant="body">Open the HireQuick mobile app to verify your identity.</Text>;
}
